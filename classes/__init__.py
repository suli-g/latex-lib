"""
Implements the latex_array module, which contains a class
for simplifying representation of NumPy arrays using LaTeX
in a Jupyter notebook.

Packages:
    - `latex_array` implements classes for representing
      arrays in LaTeX format.
"""
